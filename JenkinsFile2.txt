pipeline {
    agent {
        label 'git' 
    }

    stages {
        stage('Deploy') {
            steps {
                sh "docker run -d --name nginx -p 81:80 nginx"
            }
        }
    }
}
