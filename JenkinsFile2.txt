pipeline {
    agent {
        label 'git' 
    }

    stages {
        stage('Build') {
            steps {
                sh "hostname"
            }
        }
        stage('Test') {
            steps {
                echo 'Testing something'
            }
        }
        stage('Deploy') {
            steps {
                sh docker run -d --name nginx -p 81:80 nginx
            }
        }
    }
}
